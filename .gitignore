# Local-only working notes (not published)
tasks/
SPEC.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# uv
.uv/
uv.lock

# Env / secrets
.env
.env.local

# Per-agent last-run dumps (agent-produced JSON dropped next to agent.py)
agents/*/last_run.json

# Per-agent vector-store indexes (built at runtime; not source-controlled)
agents/*/*.db
agents/*/*.db.faiss

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
