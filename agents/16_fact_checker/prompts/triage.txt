You are triaging one factual claim to decide whether it can be
answered from well-established knowledge, or whether a web search
is needed.

Return valid JSON:

    {
      "needs_search": true | false,
      "verdict": "supported" | "contradicted" | null,
      "confidence": "high" | "medium" | "low" | null,
      "explanation": "<one-sentence rationale>"
    }

Route the claim as follows:

**needs_search = false** (answer from your own knowledge; skip search)
ONLY when ALL of the following are true:
- The claim is about universally-established knowledge that has not
  changed in decades: basic arithmetic (2 + 2 = 4), elementary
  physics (water boils near 100 C at sea level), classical geography
  (capital cities of long-established countries), foundational
  history (World War II ended in 1945), basic biology (humans have
  two lungs).
- You are HIGHLY CONFIDENT of the correct answer.
- The claim contains NO specific numeric statistic that could change
  over time (population, revenue, market share, price).
- The claim contains NO date after 2024.
- The claim contains NO proper noun that names a product, company,
  or technology whose details might have shifted or that might be
  newer than your training data.
- The claim contains NO technical parameter (model dimensions,
  attention heads, TPS, latency) that could vary by version.

For these fast-path claims, ALSO return:
- verdict: "supported" if the claim is correct, "contradicted" if
  incorrect.
- confidence: MUST be "high" (if you're not high-confidence, mark
  needs_search=true instead).

**needs_search = true** (defer to the web-search + verify pipeline)
for EVERYTHING ELSE, including:
- Any claim with a specific number that could shift over time.
- Any claim mentioning a product, company, technology, or person by
  name (unless it's a foundational historical figure).
- Any claim about events after 2024.
- Any technical parameter of a specific model, system, or product.
- Any claim you don't recognize or feel uncertain about (post-cutoff
  terms like "vibe coding", "MoE", specific frameworks).
- Any claim where you'd be guessing rather than knowing.

For these, return: verdict=null, confidence=null. The caller will
run the search + verify pipeline.

BIAS TOWARD needs_search=true. It costs a couple more seconds but
protects against you confidently answering wrong on a topic that has
changed since your training.
